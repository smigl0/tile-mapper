{
  "name": "08--tilemanager",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-docs": "typedoc --plugin typedoc-plugin-rename-defaults --options ./typedoc.json --entryPointStrategy expand src/"
  },
  "devDependencies": {
    "typedoc-plugin-rename-defaults": "^0.7.0",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "bootstrap-icons": "^1.11.3",
    "typedoc": "^0.25.13"
  }
}